<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trunk.checkfacenow"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <!-- Permisos requeridos poa la aplicación -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.checkfacenow.vistamovil.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.checkfacenow.vistamovil.CamaraActivity"
            android:label="@string/app_name" >

            <!-- configure this activity to use landscape orientation -->
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.checkfacenow.vistamovil.DetallesActivity"
            android:label="@string/title_activity_detalles" >
        </activity>
        <activity
            android:name="com.checkfacenow.conect.WSConectionActivity"
            android:label="@string/title_activity_wsconection" >
        </activity>
        <activity
            android:name="com.checkfacenow.vistamovil.CargadorActivity"
            android:label="@string/title_activity_cargador" >
        </activity>
    </application>

    <uses-feature android:name="android.hardware.screen.portrait" />

</manifest>